version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: mospan_db
    restart: always
    environment:
      POSTGRES_DB: mospan
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: